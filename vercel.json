{
  "buildCommand": "pnpm turbo run build --filter=web",
  "outputDirectory": "apps/web/build/client",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": null,
  "rewrites": [{ "source": "/((?!assets/|favicon\\.ico|robots\\.txt).*)", "destination": "/index.html" }]
}
